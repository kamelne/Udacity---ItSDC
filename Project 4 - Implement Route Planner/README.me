Uses Python and the A* algorithm to find the optimal path between two nodes on a given map. Created various funtions to support PathPlanner class, using approproate date sructures determining cost and scores of moving through the map, finally updating for the best path.
